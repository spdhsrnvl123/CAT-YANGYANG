import {useRecoilValue} from "recoil"
import {listState} from "../atoms"

const ResultList = ()=>{

    // const s = useRecoilValue(listState);
    // console.log(s)

    return(
        <div>
        </div>
    )
}

export default ResultList;